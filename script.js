$("#currentDate").text(moment().format("dddd, MMMM Do YYYY"));

// once user puts text in the input field and clicks the save button
// grab the input and store it into local storage
// from local storage grab that information and put it back into the input field

// make sure the specific input button is talking to its field. (.this())
